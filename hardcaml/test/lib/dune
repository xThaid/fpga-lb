(library 
 (name lb_dataplane_test_lib)
 (libraries base hardcaml hardcaml_verilator stdio lb_dataplane)
 (preprocess 
  (pps ppx_deriving_hardcaml ppx_jane ppx_expect)))

(include_subdirs unqualified)